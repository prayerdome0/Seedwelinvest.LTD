{
  "name": "seedwel-investment-ltd",
  "version": "1.0.0",
  "private": true,
  "description": "Seedwel Investment LTD website and secure team portal",
  "license": "UNLICENSED",
  "scripts": {
    "test": "node --test tests/*.test.js && npm run validate",
    "validate": "node tools/check-structured-data.js && node tools/check-links.js && node tools/build-certificates.js --check",
    "build:certificates": "node tools/build-certificates.js",
    "build:images": "bash tools/build-images.sh"
  },
  "dependencies": {
    "cloudinary": "^2.10.0"
  },
  "devDependencies": {
    "jsdom": "^30.0.1",
    "jspdf": "^2.5.2"
  }
}
